package demo;

public class run {
  UserInterface userInterface = new UserInterface();
    public void run(){
      userInterface.Ordering();
      userInterface.discountedPrice();
      userInterface.taxedPrice();
    }
}
